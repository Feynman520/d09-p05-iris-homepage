# 홈페이지 추가 내용 인수 (2026-09-17, P03 세션 → P05 세션)

P03(설치 패키지) 세션이 2.0.1 핫픽스를 내면서 홈페이지에 넣어야 할 내용을 정리한 문서다. P05 세션은 이 문서를
읽고 `site/index.html`·`site/install.html`·`site/site.js`(i18n 사전, ko/en 두 벌)를 고친다. 문구는 그대로 써도 되고
톤을 맞춰 다듬어도 된다. **사실 관계(숫자·동작)는 바꾸지 말 것** — 전부 실측·설계 근거가 있다.

## 0. 이미 반영된 것 (P03 세션이 08:40에 푸시)

- `site/site.js` `CONFIG.fallback.installer` = v2.0.1 · 383,132,591 B · 2026-09-16 · `IRIS-Setup_v2.0.1_2026-09-16.zip` ·
  sha256 `e3c208db5663fd4088f4ad72319d9726b659f2142aea5994f67d9fe28fa3f317`. GitHub Release `iris-installer--v2.0.1` 이 Latest.
- 남은 곳: `site/index.html` 140행 `dl-hashcmd` 예시가 아직 `v1.2.0` 파일명이면 `IRIS-Setup_v2.0.1_2026-09-16.zip` 로(자동 갱신
  코드가 있으면 그대로 둠). R2 미러 링크(`dock-dl`·`dl-btn` href 의 v1.4.5)는 2.0.x 가 R2 300MB 한도를 넘어 미러가 없으므로
  **GitHub Release 첨부로** 바꾸거나 미러 문구를 내린다(site.js 165행 주석 참조).

## 1. 설치 안내 보강 — 「서명이 없는 파일이라 윈도우가 막을 때」

`install.html` ② 「IRIS-설치.cmd 두 번 클릭」 바로 아래에 붙인다. 지금 페이지엔 SAC(`#sac`)와 SmartScreen(`inst.k1bh`) 항목이
따로 있는데, 초보 눈높이로 **순서대로 한 번에** 보이게 한다.

### 왜 막히나 (한 문단)

IRIS 설치 파일은 코드 서명(개발사 인증서로 "이 파일은 누가 만들었다"를 윈도우에 증명하는 것)이 아직 없습니다. 그래서
윈도우가 "알 수 없는 앱"으로 보고 한 번 막을 수 있습니다. 파일 자체는 GitHub 에 공개된 소스로 만든 것이고, 아래 sha256 값으로
받은 파일이 원본과 같은지 확인할 수 있습니다.

### 막혔을 때 순서 (위에서부터 해 보고, 되면 멈춘다)

```text
① 파란 창 "Windows의 PC 보호" (SmartScreen)
   → 「추가 정보」 클릭 → 「실행」 단추가 나타남 → 실행
      ↓ 창이 아예 안 뜨거나 "스마트 앱 컨트롤이 차단했습니다" 라면
② 스마트 앱 컨트롤(SAC) 끄기 — 새 PC 는 대개 켜져(또는 평가 모드) 있습니다
   설정 › 개인 정보 및 보안 › Windows 보안 › 앱 및 브라우저 컨트롤 › 스마트 앱 컨트롤 설정 › 「끄기」
   (한 번 끄면 다시 켤 수 없습니다 — 윈도우를 새로 설치하지 않는 한. 켠 채로는 서명 없는 프로그램 대부분이 막힙니다)
   → 다시 IRIS-설치.cmd 두 번 클릭
      ↓ 그래도 아무 반응이 없으면
③ 명령 창에서 직접 실행
   1. 압축을 푼 폴더를 연다
   2. 주소 표시줄(폴더 경로가 적힌 칸)을 클릭해 cmd 라고 치고 Enter → 그 폴더에서 검은 창(명령 프롬프트)이 열린다
   3. 검은 창에 다음 한 줄을 붙여 넣고 Enter:
        IRIS-설치.cmd
   4. 몇 초 뒤 브라우저가 열리며 설치 화면이 나오면 성공. 검은 창은 스스로 닫힙니다
      ↓ 검은 창에 오류가 찍히면
④ 그 화면을 사진으로 찍어(또는 글자를 복사해) 문의. zip 뿌리의 「설치가 안 되면.txt」에 같은 안내가 있습니다
```

### 받은 파일이 원본인지 확인(선택)

PowerShell 을 열고 zip 이 있는 폴더에서:

```powershell
Get-FileHash .\IRIS-Setup_v2.0.1_2026-09-16.zip -Algorithm SHA256
```

나온 값이 `e3c208db5663fd4088f4ad72319d9726b659f2142aea5994f67d9fe28fa3f317` 이면 같은 파일입니다.

### 설치 중 SAC 가 켜져 있으면 (2.0.1 신규 동작)

설치기의 사전 점검이 「스마트 앱 컨트롤이 켜져 있습니다」라고 미리 알립니다. 그 상태로도 대개 설치는 되지만, 동봉한
파이썬 도구가 차단돼 세팅이 venv(파이썬 환경) 단계에서 멈출 수 있습니다. 그러면 위 ② 대로 끄고 「다시 시도」를 누르면 됩니다.

## 2. 새 절 「IRIS 는 무엇이 다른가」 — 3열 비교

`index.html` 의 「IRIS란」과 「어떻게 돌아가나」 사이(또는 「기능」 뒤)에 넣는다. 제목 후보: **"터미널, 데스크탑 앱, 그리고 IRIS"**.
리드 문장: *같은 Claude Code 를 쓰더라도, 어디에서 어떻게 쓰느냐가 다릅니다. IRIS 는 셋 중 "일이 놓일 자리와 규칙까지 함께
설치하는" 유일한 길입니다.*

| | 터미널에서 CLI 직접 | Claude Code 데스크탑 앱 | **IRIS** |
|---|---|---|---|
| **처음 세팅** | Node·Git·Python·CLI·MCP 를 하나씩 직접 설치·설정(한글 경로·PATH 문제를 스스로 해결) | 앱 설치 한 번. 단 문서 도구·다른 에이전트는 별도 | **zip 하나 두 번 클릭.** 런타임(Node·Python·Git)·Claude Code·Codex·문서 자동화 도구·대시보드를 `C:\IRIS` 한 폴더에 자동 설치. 시스템 폴더·레지스트리 무접촉 |
| **일이 놓일 자리** | 폴더는 사용자가 알아서 | 프로젝트 폴더를 고름 | **역할 → 분야 → 프로젝트 → 단계 폴더 체계**를 미리 세워 줌(교사·연구자·사업자·개발자·사무직·학생·저자 프리셋 7종) |
| **규칙(지시 파일)** | 직접 작성·관리 | 직접 작성 | **폴더마다 `AGENTS.md` 규칙, 상위 규칙 자동 상속.** 어느 폴더에서 열든 비서가 "여기서는 이렇게"를 이미 앎 |
| **에이전트** | 한 번에 하나 | Claude 만 | **Claude Code 와 Codex 둘 다.** 같은 요청을 다른 모델로 이어 가거나, 대화 맥락을 Claude ↔ Codex 로 넘김 |
| **화면** | 검은 글자 흐름. 한 창에 한 세션 | 깔끔한 대화 창 | 깔끔한 대화 창 **+ 세션 여러 개를 한 화면에서 지휘**(카드). 창을 닫아도 세션은 살아 있고, 트레이에서 한 번에 종료 |
| **문서 작업** | MCP 를 직접 찾아 설치 | 별도 설치 | **HWP(한글)·엑셀·파워포인트·워드·PDF 자동화 도구 동봉** — 한컴·오피스가 있으면 바로, 없으면 "대기"로 두었다가 깔면 켜짐 |
| **인터넷이 없을 때** | 설치 자체가 어려움 | 설치 불가 | 오프라인에서도 ③단계(폴더·규칙·도구)까지 설치되고, 로그인·내려받기만 "남은 일"로 인수 문서에 적어 줌 |
| **투명성·되돌리기** | — | 앱 설정 위치가 흩어짐 | 전부 `C:\IRIS` 안. **폴더 하나 지우면 끝.** 설치 보고서·영수증(무엇을 어디 놓았는지)을 남김 |
| **비용** | 구독만 | 구독만 | **구독만. IRIS 자체는 무료·공개(MIT)** |
| **한계(정직하게)** | 가장 가볍고 유연 | 가장 단순 | 윈도우 10/11 64비트 전용. 첫 설치 zip 이 383MB. 코드 서명이 아직 없어 첫 실행 때 윈도우 경고를 한 번 넘겨야 함 |

주의: 데스크탑 앱 열은 "Claude Code 데스크탑 앱은 Claude 전용이고 폴더 체계·규칙 상속·Codex 연동을 제공하지 않는다"는
사실만 적는다. 그 앱의 개별 기능(diff 보기·워크트리 등)을 깎아내리는 문장은 넣지 않는다.

한 줄 요약(섹션 맨 아래): **터미널은 손, 데스크탑 앱은 창, IRIS 는 손·창·작업실을 한 번에 설치합니다.**

## 3. 「설치는 하나, 쓰는 길은 둘」 (간편 버전 대신 안내로)

간편 zip 은 보류(2026-09-17 결정). 대신 내려받기 절 아래 두 줄:

- **바로 전부 쓰기** — 설치 끝나면 로그인까지 이어서 진행(⑥).
- **일단 창·폴더·대시보드만** — 로그인·내려받기 단계는 건너뛰고 IRIS 창을 먼저 써 봐도 됩니다. 남은 일은 `C:\IRIS\_agent\setup\설치보고-*.md` 에 순서대로 적혀 있고, IRIS 창의 「설치 이어 하기」로 언제든 마저 합니다.

## 4. 2.0.1 이 무엇을 고쳤나 (「구성 요소와 새 소식」 절용, 짧게)

- 한컴오피스가 없는 PC 에서 설치가 44%에서 멈추던 결함 수정(2.0.0 사용자는 2.0.1 로 다시 설치 권장).
- Claude Code 내려받기 뒤 첫 실행 확인 실패, 느린 PC 에서 마지막 검사 시간 초과, 스마트 앱 컨트롤 안내 등 12건.
- 근거·전문 = 릴리스 노트 `https://github.com/Feynman520/d09-p03-iris-installer/releases/tag/iris-installer--v2.0.1`.

## 5. 배포 뒤 확인

- `https://iris-workspace.com` 내려받기 단추가 **v2.0.1 / 383.1 MB / 2026-09-16** 을 보이는지, sha256 이 위 값인지.
- R07 배포스택 지도(`R07-개발자(Developer)\배포스택(Deploy-Stack)\배포스택-지도(Deploy-Stack-Map).md`)에 2.0.1 릴리스 1행 추가(규칙: 새 판마다).
